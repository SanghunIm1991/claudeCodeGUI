# 申し送り事項

## 現在の申し送り

- 要件定義工程: **完了**
- コンポーネント設計工程: **完了**
- 関数設計工程: **進行中。COMP-01〜COMP-11は完了（論理・可読性の両フェーズとも収束）。COMP-12〜COMP-17（6コンポーネント分）は未着手**
  - COMP-11（`Program.cs`エンドポイント拡張・DI配線・起動時処理）: 論理ラウンド1で軽微指摘1件（traceability_matrix.md②表のCOMP-11行が空欄のまま。内容面の矛盾を伴わない単純な反映漏れ）→修正（②表にCOMP-11の関数一覧を追記）→再レビュー省略で論理設計フェーズ収束（内容確定コミット`cf70d78`）。COMP-01（`DefaultPermissionMode`）・COMP-08（`Issue.CurrentStage`）双方からの申し送り事項に対し、いずれも新規バリデーション関数（`IssueUpdateValidator.IsKnownPermissionMode`・`IsKnownStage`、静的純粋関数）を`PUT /api/issues/{id}`ハンドラ内で呼ぶ形で追加すると確定（入力境界で一度だけ検証する方針。CON-04のプルダウン選択肢・COMP-08前提の5Stage値とそれぞれ整合することをレビューで確認済み）。可読性ラウンド1で段落分割4箇所・Mermaid図2件（起動時処理のsequenceDiagram、PUT /api/issues/{id}検証順序のflowchart）を追加し、独立レビューで意味的ドリフトなし・指摘ゼロを確認して収束（コミット`ce228ad`）。COMP-11の関数設計は完了
  - COMP-10（`OrphanSweepService`、新規）: 論理ラウンド1で重大指摘2件（①`OrphanDetection.Detect`の`allRuns`null時の例外種別誤記。`NullReferenceException`ではなく実際はLINQ `Enumerable.Where`により`ArgumentNullException(nameof(source))`。②監査ログ追記失敗時の自己修復クレームの破綻。ログ・json両移動成功直後の監査ログ追記のみが失敗するケースでは、jsonが既に退避済みのため次回`SweepAsync`でも再検出されずREQ-25の監査証跡が永久に欠落する未検討ケース）→修正（指摘②は方針(b)採用: 監査ログ追記失敗のみ`catch`して警告継続、ログ・json移動の失敗は従来通り例外伝播で分離。「移動順序の設計判断」節に3手順比較表を新設）→ラウンド2で反映確認OK・新規指摘なしにより論理設計フェーズ収束（コミット`2d83004`）。可読性ラウンド1で表変換1箇所・段落分割2箇所・表記統一2箇所（「削除」→実処理に即した「退避/移動」）を実施し、独立レビューで意味的ドリフトなし・指摘ゼロを確認して収束（コミット`ec6a65c`）。COMP-10の関数設計は完了
  - COMP-01: 論理ラウンド1で重大指摘2件→修正→ラウンド2収束（指摘なし）。可読性ラウンド1で軽微指摘2件→修正→再レビュー省略で完了
  - COMP-02: 論理ラウンド1で重大1・軽微2件→修正→ラウンド2で軽微1件（新規）→修正→再レビュー省略で完了。可読性ラウンド1で指摘なし・収束
  - COMP-03: 論理ラウンド1で指摘なし・収束。可読性ラウンド1で軽微指摘1件→修正→再レビュー省略で完了
  - COMP-04: 論理ラウンド1で軽微指摘1件→修正→再レビュー省略で論理設計フェーズ収束（コミット`24c4336`）。可読性ラウンド1でMermaid図追加・段落分割を実施し、独立レビューで意味的ドリフトなし・指摘ゼロを確認して収束（コミット`9bc87f1`）。COMP-04の関数設計は完了
  - COMP-05（`ClaudeRunEngine`拡張、排他制御・キャンセル競合修正を含む複雑な部分）: 論理ラウンド1で重大指摘1件（`StartAsync`正常系でRunが2回保存される順序矛盾、component_design.md/COMP-02節と不整合）→修正→ラウンド2で軽微指摘1件（新規。`prompt`引数の構築ステップ記載漏れ）→修正→再レビュー省略で論理設計フェーズ収束（コミット`c6d31ca`）。可読性ラウンド1で軽微指摘1件（見出しの語順の表記揺れ）→修正→再レビュー省略で収束（コミット`3169761`）。COMP-05の関数設計は完了
    - 特記事項: 論理設計ラウンド1の内容コミット（`6f76f4d`）のコミットメッセージが誤って「COMP-04 appsettings.json拡張の関数設計を追加」となっているが、実際の変更内容（diff）はCOMP-05 `ClaudeRunEngine`拡張の関数設計であり、内容自体に誤りはない。コミットメッセージ書き換え系操作（amend等）は禁止のため訂正コミットは行っていない。セッション再開手順でコミット履歴を辿る際、この1件のみメッセージと内容が食い違う点に注意
  - COMP-06（`MockRunGenerator`新規）: 論理ラウンド1で指摘なし・収束（コミット`fdaaceb`）。可読性ラウンド1で軽微指摘2件（Mermaid矢印ラベルの表記揺れ・既存図とのスタイル不統一）→修正→再レビュー省略で収束（コミット`b55f279`）。COMP-06の関数設計は完了
  - COMP-07（`TargetPathValidator`新規、パス検証というセキュリティに関わる部分）: 論理ラウンド1で重大指摘1件（`Path.GetFullPath("")`が例外を投げずカレントディレクトリを返すという事実誤認。実際は`ArgumentException`を送出）・軽微指摘1件（例外契約の記載不足）→修正→ラウンド2で反映確認OK・新規指摘なしにより論理設計フェーズ収束（コミット`452a9bb`）。可読性ラウンド1で表への整理・Mermaid判定フロー図を追加し独立レビューを実施した結果、軽微指摘1件（`PickRoot`ノードに`allowedRoots`側の`ArgumentException`注記が漏れている）が判明し、次回セッションで修正・COMP-07の関数設計を完了した。修正時、軽量モデルによる初回修正が`Normalize`ノードのスタイルを模倣し誤って境界値番号「（#11）」を付与（#11は`targetPath`側の空文字列パターンであり`allowedRoots`側に対応する境界値パターンは表に存在しない）していたことをオーケストレーターの確認で発見し、追加の軽量修正で削除。いずれも軽微指摘のため再レビュー省略で可読性フェーズ収束
  - COMP-08（`LoopEngine`新規、自律ループの中核・排他制御と判定ロジックを含む最も複雑な部分）: 論理ラウンド1で重大指摘2件（`Evaluate`境界値表#8行の比較式・真偽・結果の自己矛盾、`CurrentStage`書き込み経路が本コンポーネント自身のみという事実誤認〈実際はPUT /api/issues/{id}ハンドラにも未検証の書き込み経路が存在〉）→修正→ラウンド2で軽微指摘1件（新規。境界値表#7・#8の説明文の精度）→修正→再レビュー省略で論理設計フェーズ収束（コミット`2eaa608`）。可読性ラウンド1で段落分割・Mermaid図4件（ロック区間共通パターン、HandleRunCompletedAsync/StartLoopAsync/StopLoopAsync各処理フロー）を追加し独立レビューで意味的ドリフトなし・指摘ゼロを確認して収束（コミット`2937c51`）。COMP-08の関数設計は完了。`DefaultPermissionMode`（COMP-01由来）・`CurrentStage`（COMP-08で新規発見）双方の妥当性検証欠落は、いずれもCOMP-11（`PUT /api/issues/{id}`ハンドラ）の関数設計時に要否を判断する旨を申し送り済み
  - COMP-09（`RetentionPruner`新規、保持件数プルーニング）: 論理ラウンド1で軽微指摘1件（セクション番号の誤引用。「2.5.2節境界値#6」は誤りで正しくは「2.5.1節境界値#6」）→修正→再レビュー省略で論理設計フェーズ収束（コミット`2cfaa4a`）。可読性ラウンド1で段落分割・比較表化・Mermaid図追加を実施し独立レビューで意味的ドリフトなしを確認、軽微指摘1件（参照ラベル「削除失敗時の扱い」が実見出し名と字面不一致。論理設計フェーズから既存）→修正→再レビュー省略で可読性フェーズ収束（コミット`adb1e51`）。COMP-09の関数設計は完了。削除失敗時の扱い（ログファイル→ストアレコードの順で削除しキャッチせず再送出、未完了時は次回トリガーで自己修復）を新規の設計判断として明記済み
  - 詳細な往復記録は`docs/review_log.md`「関数設計（論理）」「関数設計（可読性）」の各行を参照
  - COMP-12（`app.js` SSE自動再接続・実行中Run検出）: 論理ラウンド1〜3で重大指摘4件（onerror再接続の競合状態の未対応判断、issueId/runId偶然再一致による過剰破棄の境界ケース漏れ等）→修正（対象一致ガード＋世代一致ガード`connectionGeneration`の併用方式）→ラウンド3で反映確認OK・新規指摘なしにより論理設計フェーズ収束（コミット`213daf3`）。可読性ラウンド1で表2件・Mermaid図2件追加を実施し、独立レビューで意味的ドリフトなしを確認、軽微指摘1件（見出しレベルスキップ h3→h5）→軽量モデル(haiku)で修正→再レビュー省略で可読性フェーズ収束（コミット`8d14838`）。COMP-12の関数設計は完了
- **次にやること（次回セッション再開時の最初の一手）**: COMP-12は論理・可読性の両フェーズとも完了（コミット`8d14838`まで）。次はCOMP-13（排他制御拒否時のUX誘導、`app.js`。`docs/02_component_design/component_design.md` 661行目〜）の関数設計（論理フェーズ）に着手する。以降COMP-14〜COMP-17まで順次進め、完了すれば関数設計工程が完了し、実装工程へ自動的に進む
- 未完了: 関数設計（COMP-13〜COMP-17の5コンポーネント分）、実装、テスト、デプロイの全工程
- 特記事項: セッション中、あるレビューサブエージェントの報告文に本アプリのドメイン用語`bypassPermissions`（パーミッションモードの選択肢名）が含まれており、harnessのパターン検出が誤って権限バイパス指示として一度警告フラグを立てた。内容確認の結果、実質的な問題はなし（誤検知）

## 過去の申し送り（解決済み・直近2件のみ。それ以前は`docs/handoff_archive.md`参照）

- **2026-08-29 10:57頃 トークン逼迫によるセッションの唐突終了**: COMP-07〜COMP-10の関数設計を進めていたセッションが、COMP-10論理設計フェーズの開発サブエージェント成果物をコミット（`2ef8575`）した直後、レビューサブエージェント呼び出し前に唐突終了した。当時導入済みだった「消費率80%超過で機械的に安全停止する」セーフティ弁（同日09:38頃導入）が発動せず、`docs/handoff.md`への中断記録もないまま終了しており、サブエージェント呼び出し1回の実行中に消費率が閾値をジャンプして跨いだ可能性が高い。後続セッションで発覚し、対策として閾値を70%に引き下げ・サブエージェント報告の簡潔化指示を追加（`docs/qa_log.md`2026-08-29欄2件目に詳細記録、`~/.claude`のスキル/エージェント定義ファイルに反映・push済み）。データ損失や手戻りは発生しておらず、COMP-10論理設計フェーズのレビューから再開すればよい状態。
- **2026-08-29 トークン逼迫による中断（3回目、未コミット差分を伴う新パターン）**: COMP-11完了後、COMP-12（SSE自動再接続・実行中Run検出、`app.js`）の論理設計フェーズを3ラウンドで収束させ（コミット`213daf3`）、続けて可読性向上フェーズの開発サブエージェント成果物（表化・段落分割・Mermaid図2件追加）まで生成した直後にセッションが中断した。過去2回（2026-08-27・2026-08-29 10:57頃）と異なり、今回は独立レビュー前の成果物が`git status`上コミットされずに作業ツリーへ残った状態で中断しており、「消費率70%閾値のセーフティ弁」が今回も自発的に働かなかった可能性がある（本セッションはユーザーからの明示的な停止依頼を受けて対応した経緯であり、セーフティ弁自体が発動したかどうかは前セッションのログが手元にないため未確認）。本セッションでの対応: 未コミットだった可読性向上フェーズ成果物を「未レビュー」と明記した上でWIPコミット（`eddc8fa`）し、データ損失リスクを解消。次回セッションはこのコミットに対する独立レビューから再開すればよい状態（詳細は上記「現在の申し送り」参照）。**申し送り事項**: セーフティ弁が3回連続で発動しなかった（または発動有無が確認できない）実態がある。次回、自律ループモードでオーケストレーターを稼働させる際は、`waterfall-loop-orchestrator`エージェント自身がセーフティ弁を実際にトリガーできているかを、サブエージェント呼び出し前後の`<total_tokens>`推移を明示的にログへ残す形で検証することを推奨する。

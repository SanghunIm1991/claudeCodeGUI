# QA記録

人間の判断・承認が必要になった事項（セットアップ時の合意事項、セーフティ弁到達時のエスカレーションなど）を記録する。自動レビューの往復記録は `docs/review_log.md` を参照。

| 日付 | 工程 | 質問内容 | 回答内容 |
|---|---|---|---|
| 2026-08-24 | セットアップ | レビュー方式をどうするか | 自律ループモード（オーケストレーター＋独立サブエージェント方式）を採用 |
| 2026-08-24 | セットアップ | 再レビューの上限回数 | 3回 |
| 2026-08-24 | セットアップ | 「レビュー指摘ゼロ＝工程自動移行」の適用範囲 | 全工程に適用（テスト工程完了等の節目でも人間の最終確認を挟まない） |
| 2026-08-24 | セットアップ | 再レビュー上限到達時、残存指摘が全て軽微な場合の扱い | 既定通り自動的に次工程へ進める（`docs/review_log.md`・`docs/handoff.md`に経緯を記録） |
| 2026-08-24 | セットアップ | 開発・レビューサブエージェント往復の進捗報告頻度 | 一定時間超過時のみ報告（目安2時間。当該工程の最初のコミット時刻からの経過時間で判定） |
| 2026-08-24 | セットアップ | 既存の要件確定内容（`docs/architecture-overview.md`4.1〜4.9節、いずれも「要件確定・未実装」）の扱い | 要件定義工程の開発サブエージェントが、これらを出発点として`docs/01_requirements/`のREQ/NFR/CON形式に正式化する（内容の再検討・蒸し返しはしない。フォーマット変換と要件間の整合性確認が目的） |
| 2026-08-24 | セットアップ | Git push運用方針 | 未合意のため`git-conventions`スキルの既定（都度確認）を暫定適用。自律ループ実行中はpushを行わずコミットのみに留め、ユーザーがループを止めた・確認しに来たタイミングでまとめてpush可否を確認する |
| 2026-08-25 | コンポーネント設計（論理） | 再レビュー上限3回到達・重大指摘2件（手動中止時のLoopStopReason競合、ロジック層/HTTPハンドラ層分離方針の内部矛盾）残存時の対応方針 | (a) 上限を超えて追加の修正ラウンド（開発サブエージェント→再レビュー）をもう1回だけ許可する |
| 2026-08-25 | コンポーネント設計（論理） | 特例ラウンド4で新設したLoopEngine排他ロックにロック解放漏れ（重大指摘1件）が残った場合の対応方針 | (b) try/finallyでクリティカルセクション全体を一律カバーする方針をこちらで指定して収束させる。あわせて軽微指摘1件（既定テンプレート不在時の戻り値仕様未規定）も解消させる |
| 2026-08-27 | 関数設計（進行中、工程横断の運用見直し） | 排他制御・ロック解放漏れ系の指摘がCOMP-05/COMP-08で3回再発しエスカレーション2回に至った実績を踏まえた汎用ルール見直しの提案（レビュー観点の明文化、handoff.mdアーカイブの前倒し実施） | 両方採用。`waterfall-dev-workflow`スキルの`review_rubric.md`「コンポーネント設計」章に排他制御・ロックの完全性を独立観点として追加（本プロジェクト固有でなく全プロジェクト共通のスキルファイルに反映）。`docs/handoff.md`の解決済み申し送り5件のうち直近2件を残し、古い3件を`docs/handoff_archive.md`に切り出した |
| 2026-08-29 | 関数設計（進行中、工程横断の運用見直し） | 過去2回（2026-08-26・2026-08-27）のトークン逼迫中断がいずれもユーザーからの中断依頼を受けて止まったもので、セーフティ弁の「残りが少なくなったと判断した場合」という主観基準では自発的に発動していなかった（実態は動作していない）ことの指摘、および客観的な数値基準への修正依頼 | `waterfall-dev-workflow`スキルの`references/loop_mode.md`「セーフティ弁」章2項と、`waterfall-loop-orchestrator`エージェント定義（`.claude/agents/waterfall-loop-orchestrator.md`）「サブエージェント呼び出し前のトークン確認」章を修正（全プロジェクト共通のファイルに反映）。ツール結果に付随する`<system-reminder>`内の`<total_tokens>N tokens left</total_tokens>`をセッション開始時に基準値として記録し、以後表示されるたびに消費率を算出、**消費率80%超過**を機械的トリガーとして安全な中断手順に入る方式に変更（旧基準は「残量1割未満」を主観判断で運用していた） |
| 2026-08-29 | 関数設計（進行中、工程横断の運用見直し） | 同日09:38頃に導入した消費率80%閾値の機械的トリガーが、その後の10:03〜10:57のセッション（COMP-07〜COMP-10）でも機能せず、`docs/handoff.md`への記録なしにセッションが唐突終了した（直近コミット`2ef8575`はCOMP-10開発成果物のみでレビュー未実施のまま途切れていた）ことへの追加対策の提案・承認依頼 | 提案を採用。(1) 閾値を80%→**70%**に引き下げ（サブエージェント呼び出し1回の実行中に消費率が閾値を跨いでジャンプし得るため、その分のバッファを確保）。(2) サブエージェント（特にレビュー）への報告指示を簡潔化し、詳細根拠は記録ファイルに直接書かせることで呼び出し1回あたりの消費ジャンプ自体を抑制。`loop_mode.md`「セーフティ弁」章・`waterfall-loop-orchestrator.md`「サブエージェント呼び出し前のトークン確認」章に反映済み。唐突終了時の実害緩和（コミット粒度細分化＋セッション再開手順）は既存のまま維持 |
| 2026-08-29 | 関数設計（進行中、中断からの復旧） | COMP-12可読性向上フェーズの成果物が未コミット・未レビューのまま残った状態でセッションが中断した（前回導入した消費率70%セーフティ弁が今回も働いたか未確認）。安全な停止方法の提案依頼を受け、未コミット差分の扱いを提示 | (a) 「未レビュー」と明記した上でWIPコミットする、を採用（(b) 未コミットのまま停止、は不採用）。コミット`eddc8fa`として反映。データ損失リスクを解消した上でセッションを終了し、次回は当該コミットへの独立レビューから再開する方針を`docs/handoff.md`に記録した |
| 2026-08-29 | 汎用設定への反映（`~/.claude`リポジトリ） | 上記対応を全プロジェクト共通のloop_mode.mdへ反映する際、`--author`指定漏れでコミット`dfcbc8d`にユーザー実名・実メールアドレスが混入し、さらに誤ったauto memory（push都度確認不要という誤記載）を根拠に確認なしで`git push`してしまった。`/review-session`の独立レビューでも重大指摘として支持された。author不整合はamend/force push（禁止操作）なしには修正不能なため、対応方針の承認を仰いだ | このまま許容（推奨案を採用）。リポジトリはPrivate確認済みで内容も機微情報ではなく、`D:\claudeCodeGUI`側でも過去に同種の逸脱（コミット`34f9906`）を許容した前例がある（[[feedback_git_commit_convention]]参照）。無確認pushの根拠となった誤ったauto memoryは訂正済み（`reference_dotclaude_repo_push_policy.md`） |
| 2026-08-29 | 汎用設定への反映（`~/.claude`リポジトリ） | `/review-session`の独立レビューで、本セッションとは無関係の未コミット差分（`review_rubric.md`、2026-08-27付qa_log.mdに「反映済み」と記録されているが実際は未コミットのまま放置）が新たに見つかった。扱いの確認を仰いだ | 内容確認の上、今回のセッションでコミットする、を採用 |
| 2026-08-30 | 関数設計（進行中、工程横断の運用見直し） | 前回セッションがトークン上限で中断（4回目、review_log.md追記直後）。原因分析の結果、コンポーネント単位へのタスク分割（工程内の呼び出し粒度）は既に最も細かいが、成果物である`function_design.md`が単一ファイルのまま3109行まで肥大化しており、①サブエージェント呼び出し1回あたりの内部トークン消費が増大しジャンプ幅の閾値超過リスクを高めている、②COMP-15で実際に発生した「対応ID行の挿入位置取り違え」のような構造的ミスを誘発している、という2つの実害が確認できた。対策として(1)関数設計書のコンポーネント単位ファイルへの分割、(2)セーフティ弁閾値のさらなる引き下げ、を提案 | (1)は既存COMP-01〜15分も含めて全面的に分割し直す方針を採用（`docs/03_function_design/COMP-xx.md`に分割、`function_design.md`は索引化。分割時にCOMP-15の重大指摘1件〈対応ID行の孤立〉が構造修正として同時に解消。詳細は`docs/review_log.md`該当行参照）。(2)は70%のまま様子見（採用せず） |
| 2026-09-03 | 関数設計（進行中、COMP-15〜17）／セッション運用 | ユーザーからの「安全なタイミングで終了してほしい」という依頼を受けた際、既に稼働中だった`waterfall-loop-orchestrator`エージェント（`a1ebf96759cc5506a`）へ`SendMessage`で再開指示すべきところ、誤って`Agent`ツールで新規の同エージェント（`af0233ba12152f115`）を追加起動してしまった。新規エージェントは実行中の子サブエージェント（design-doc-readability-editor）の存在を知らず、同一ファイルへの競合作業リスクがあった。オーケストレーター側では気づけない種類のミス（呼び出し元の手順ミス）のため記録要否を確認したところ、`/review-session`の独立レビューでも実害なし（`TaskStop`でファイル書き込み前に即時停止、`git reflog`上も影響なしと裏付け済み）と判定された軽微事案について、記録推奨の所見を受けた | 記録する、を採用（本行として追記）。実害はなかったが、次回以降「既存の稼働中エージェントを再開させる場面ではSendMessage、まだ存在しないエージェントを新規に立てる場面のみAgent」という区別を意識する教訓として残す |

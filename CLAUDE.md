# プロジェクト概要

Claude Codeを用いたウォーターフォール型開発の練習を兼ねた、`waterfall-dev-workflow`スキル自体の運用を支援するローカル実行のWebアプリ「claudeCodeGUI」。ASP.NET Core (C#) 製バックエンド＋素のHTML/CSS/JSフロントエンドで、Issue登録・工程ごとのプロンプトテンプレート実行・成果物閲覧を行う（詳細は `README.md`・`docs/architecture-overview.md` を参照）。

最小構成のプロトタイプは実装・動作確認済み（2026-08-16頃まで）。本ドキュメント運用開始時点（2026-08-24）では、本番実装前の要件レビューで確定した9件の追加要件（後述）が「要件確定・未実装」の状態にあり、これらの実装が本ワークフローで進める対象。

## レビュー方式

**自律ループモード**を採用する（2026-08-24、`docs/qa_log.md`参照）。オーケストレーター（メイン会話）が開発サブエージェントとレビューサブエージェントを`Agent`ツールで独立に呼び出し、レビュー指摘がゼロになるまで自動で修正・再レビューを繰り返し、指摘ゼロで次工程へ自動的に進む。詳細は`waterfall-dev-workflow`スキルの「自律ループモード」章および`references/loop_mode.md`を参照。

- 再レビューの上限回数: **3回**
- 「指摘ゼロ＝自動進行」の適用範囲: **全工程に適用**（テスト工程完了等の節目でも人間の最終確認を挟まない）
- 上限到達時、残存指摘が全て軽微であれば: **既定通り自動的に次工程へ進める**（`docs/review_log.md`・`docs/handoff.md`に経緯を記録）
- 進捗通知: **一定時間超過時のみ**（目安2時間。当該工程の最初のコミット時刻からの経過時間で判定）
- `docs/review_log.md`（レビュー往復の記録）を`docs/qa_log.md`（人間の判断記録）と役割分担して運用する

## 開発プロセス

以下の順に工程を進める。自律ループモードのため、各工程はレビューサブエージェントの指摘ゼロ（またはすべて軽微で再レビュー省略）判定をもって次工程へ自動的に進む。

1. 要件定義 (`docs/01_requirements/`)
2. コンポーネント設計 (`docs/02_component_design/`)
3. 関数設計 (`docs/03_function_design/`)
4. 実装 (`src/ClaudeCodeGui/`)
5. テスト (`docs/04_test/` に仕様書、`tests/` にテストコード)。テストレベル定義・由来管理・リグレッション方針など戦略の詳細は `test-strategy` スキルに従う
6. デプロイ (`docs/05_deployment/`) — テスト完了後、デプロイ形態をユーザーと合意してから着手する

各工程の成果物には、要件定義書のID（REQ-xx / NFR-xx / CON-xx）を起点としたIDを付与し、`docs/traceability_matrix.md` に反映すること。

関数設計・実装・テストの各工程は、コンポーネント（COMP-xx）単位で分割し、コンポーネントごとに個別の開発→レビューサイクルを回す（`references/loop_mode.md`「工程内の呼び出し粒度」章）。コンポーネント設計・関数設計は論理面／可読性面の2段階パイプラインで進める（同ファイル「設計工程の論理/可読性2段階パイプライン」章）。

## 対象とする追加要件（要件定義工程の出発点）

`docs/architecture-overview.md` の「4. 主な設計判断」節（4.1〜4.9）に、2026-08-24時点で要件確定・未実装の状態にある以下9件がまとめられている。要件定義工程の開発サブエージェントは、これらを**出発点として**`docs/01_requirements/`のREQ/NFR/CON形式に正式化する（内容の再検討・蒸し返しは行わず、フォーマット変換と要件間の整合性確認を主目的とする。ただし整合性確認の過程で矛盾・欠落が見つかった場合は通常のレビュー指摘として扱ってよい）。

1. モック実行モード（4.1）
2. GUI配置の改善（4.2。配色バグは実装済み・コミット済み。配置の改善方針が対象）
3. パーミッションモード運用方針・`TargetProjectPath`の範囲制限（4.3）
4. SSE自動再接続（4.4）
5. 同一Issueへの同時実行の排他制御（4.5）
6. 自律ループモード（4.6。**本アプリの機能としての**自律ループモードであり、本CLAUDE.mdが定めるClaude Code側の開発ワークフローの自律ループモードとは別物）
7. Run/ログの保持方針（4.7）
8. Issue削除時に孤児化するRun/ログの扱い（4.8）
9. 自動テスト方針（4.9）

## 現在の進捗

- [x] 自律ループモードのセットアップ（CLAUDE.md・`.claude/settings.json`・`docs/`配下の骨組み作成、承認チェックリスト確認）— 2026-08-24
- [ ] 要件定義
- [ ] コンポーネント設計
- [ ] 関数設計
- [ ] 実装
- [ ] テスト
- [ ] デプロイ

## 確定した仕様

- バックエンド: ASP.NET Core (C#)
- フロントエンド: 素のHTML/CSS/JavaScript（`wwwroot/`配下、ビルドステップなし）
- 実行エンジン: `claude` CLIをheadlessモード（`-p`等）でサブプロセス起動
- 対応OS: Windows（開発機はWindows 11）
- 上記9件の追加要件それぞれの詳細な確定内容は `docs/architecture-overview.md` 4.1〜4.9節を参照（要件定義工程で`docs/01_requirements/`に正式化後は、そちらを正とする）

## 実装環境

- `dotnet` SDK: 8.0.408 / 9.0.314 導入済み
- `claude` CLI: `C:\Users\21452\.local\bin\claude.exe`
- 起動コマンド標準形: `dotnet build src\ClaudeCodeGui\ClaudeCodeGui.csproj` / `dotnet run --project src\ClaudeCodeGui\ClaudeCodeGui.csproj`
- テスト実行標準形（テストプロジェクト追加後）: `dotnet test src\ClaudeCodeGui.Tests\ClaudeCodeGui.Tests.csproj`（xUnitベースで新規追加予定。4.9節参照）
- PowerShellツールはツール呼び出しごとにシェル状態がリセットされる。`dotnet`等のPATHが解決できない場合はユーザーのグローバルCLAUDE.md記載のPATH復元コマンドを使う

## トレーサビリティマトリックス

要件から実装・テストまでの対応関係を追跡できるよう、`docs/traceability_matrix.md` を工程が進むたびに追記・更新する（工程ごとに別ファイルを作らず、1ファイルで育てていく）。

- ①要件×コンポーネント、②コンポーネント×関数、③関数×テスト、④要件×テスト（直接検証トレース）の4表構成とする
- 各表は上流工程のIDを行、下流工程のIDを列に配置し、交点に「○」を付ける
- ③④はテストモジュール（テストファイル）単位で列を持ち、セルに該当テストIDを列挙する
- テスト工程完了時、④の全要件ID行に最低1つのテストIDが記載されていることを確認する

### ID対応関係の一次情報源・記載方針（可読性）

一次情報源（single source of truth）は `docs/traceability_matrix.md` のみとする。各工程の成果物本文には対応IDの簡潔な一覧のみを記載し、全IDを毎回フルの理由付きで書き下さない。対応関係が自明でないIDのみ個別に理由を補足する。テストコードのdocstring等にはテストIDと短い要約のみを記載し、要件ID・関数IDのフル一覧は重複記載しない。複数コンポーネントにまたがる依存関係・シーケンスはMermaidでの図示を検討する。

## 品質方針（テスト容易性）

- ロジック（`ClaudeRunEngine`のプロンプト組み立て・状態遷移判定、`ArtifactService`のパス検証等）とHTTPハンドラ/表示処理を分離し、ロジック層は単体テスト可能な構成にすること
- 関数・メソッドは入力と出力（戻り値）が明確な単位に分割し、副作用とロジックを混在させないこと
- 関数設計書には各関数の入出力仕様をテストケース設計に転用できる粒度で記載すること

## テスト戦略

テストレベル定義（単体/結合/統合）・由来管理・カバレッジ方針・リグレッション方針・GUIテストの実行主体は `test-strategy` スキルおよび `docs/architecture-overview.md` 4.9節に従う。テスト仕様書（`docs/04_test/`）のテストケース一覧にはテストID・対応要件ID・対応関数ID・テストレベル・由来を列として持たせる。

- リグレッション方針: プロトタイプ規模のため当面はフルリグレッション（テスト件数が増え実行時間が問題になった時点で代表ケースリグレッションの導入を検討する）
- TDD/テスト作成タイミング: デフォルト通り、設計書ベースの後付けテストとする

## 変更操作の承認について

プロジェクトフォルダ内の変更はGitで追跡・復元可能なため、以下は事前の承認確認を省略してよい。

- ドキュメント・ソースコードの編集などGitで追跡可能な個別のファイル変更操作
- 上記を確定させる `git commit`

ただし、以下は引き続きユーザーの明示的な承認を得てから行う。

- 開発プロセスの工程間ゲート承認に相当する事項（自律ループモードでは通常レビューサブエージェントの指摘ゼロ判定に置き換わるが、セーフティ弁到達時などは人間確認が必要）
- Gitで追跡されない・復元不能な操作（`git push`、ファイル削除、外部システムへの送信など）

**push運用方針**: 2026-08-24時点で未合意のため、`git-conventions`スキルの既定（都度確認）を暫定適用する。自律ループ実行中はpushを行わずコミットのみに留め、ユーザーがループを止めた・確認しに来たタイミングでまとめてpush可否を確認する。

### 厳格に禁止するGit操作

以下はユーザーの承認の有無にかかわらず実行しない（承認確認をしてよいかを尋ねることもしない）。

- マージ系コマンド（`git merge` など）
- 過去のコミットを書き換えるコマンド（`git commit --amend`、`git rebase`、`git filter-branch`、`git push --force`系、`git reset --hard` など）

`.claude/settings.json` の `permissions.deny` に上記を登録済み（技術的にもブロック）。

## プログラム実行の承認について

設計書に基づいて作成したプログラム（`src/ClaudeCodeGui/`の実装コード、`tests/`のテストコード）を動作確認・レビューのために実行する操作（`dotnet build`/`dotnet run`/`dotnet test`等）は、プロジェクトフォルダ外のファイルを書き換える場合を除き、都度の承認確認を省略してよい。実際に使用するコマンドパターンは `.claude/settings.json` の `permissions.allow` に登録済み（`dotnet build *` / `dotnet run *` / `dotnet test *`、Bash/PowerShell双方）。

### テスト実行コマンドの標準化

テスト実行は「実装環境」章の標準形コマンドのみを使う。作業ディレクトリを変えてからの`&&`連結や、シェルループでの複数回実行、絶対パス直指定は避ける。

## セッション運用方針

1. **即時着手**: 指示を受けたら、作業概要の提示や承認を待たずファイル作成・編集にすぐ着手してよい（工程間ゲート承認など明示的承認が必要な事項を除く）
2. **完了時の挙動**: 指示された作業が正常に完了したら、完了報告した上で待機する
3. **申し送り**: ユーザーの明示的承認が必要な事項が残っている場合、その旨を提示するとともに `docs/handoff.md` に次回セッションへの申し送り事項を記録する

## 判断・承認のQA記録

Claudeがユーザーに判断や承認を仰ぎ、ユーザーが回答した場合は、その要点を `docs/qa_log.md` のQA表に追記する。プロジェクトルールそのものの見直しが発生した場合も決定事項としてここに残す。

## Gitコミット規約

- コミットメッセージの先頭に `[claude] ` を付ける
- コミット時に `--author="ClaudeCode <noreply@anthropic.com>"` を指定する（コミッターは変更しない）。既存コミット履歴（`330a457`等）で使われている表記に合わせる
- 例:
  ```powershell
  git commit -m "[claude] 要件定義書を作成" --author="ClaudeCode <noreply@anthropic.com>"
  ```
- ユーザー自身が手動でコミットする場合は、通常通りそのマシンのグローバルGit設定でコミットしてよい（接頭辞不要）

## 環境メモ

このマシン特有の環境上の注意点（PATHリセット、Python複数バージョン競合など）はユーザーのグローバルCLAUDE.md「環境メモ」章に集約されている。本プロジェクト固有の追加事項はない。

## フォルダ構成

```
docs/
  architecture-overview.md   既存の設計概要資料（4.1〜4.9に要件確定内容の出発点あり）
  class-function-design.md   既存のクラス・関数逆引きリファレンス
  guide.html                 既存の操作手順書
  01_requirements/            要件定義書
  02_component_design/        コンポーネント設計書（基本設計）
  03_function_design/         関数設計書（詳細設計）
  04_test/                    テスト仕様書
  05_deployment/               デプロイマニュアル（テスト完了後に作成）
  traceability_matrix.md      トレーサビリティマトリックス（全工程共通・1ファイル）
  qa_log.md                    判断・承認のQA記録
  review_log.md                レビュー往復の記録（自律ループモード）
  handoff.md                   セッション間の申し送り事項
src/ClaudeCodeGui/            既存の実装コード（本ツール自体のASP.NET Coreプロジェクト）
tests/                        テストコード（新規追加予定）
demo/                         本ツールが対象プロジェクトとして生成したHello Worldアプリの実例。本ツール自体のsrc/testsではない
```
